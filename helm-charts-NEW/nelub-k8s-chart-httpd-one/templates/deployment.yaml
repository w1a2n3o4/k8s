apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-test-httpd-one
  labels:
    app : {{ .Release.Name }}-k8s-test-httpd-one
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      project: {{ .Release.Name }}
  template:
    metadata:
      labels:
        project: {{ .Release.Name }}
    spec:
      containers:
        - name : {{ .Release.Name }}-httpd-one
          image: {{ .Values.container.image }}
          ports:
            - containerPort: 80
