apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-auto-test.fullname" . }}-test-connection"
  labels:
    {{- include "chart-auto-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-auto-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
