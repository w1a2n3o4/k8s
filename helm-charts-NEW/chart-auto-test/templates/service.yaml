apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-auto-test.fullname" . }}
  labels:
    {{- include "chart-auto-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-auto-test.selectorLabels" . | nindent 4 }}
